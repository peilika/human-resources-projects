<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - HRUI</title>
    <link rel="stylesheet" href="https://unpkg.com/element-ui/lib/theme-chalk/index.css">
    <script src="https://unpkg.com/vue@2.6.14/dist/vue.min.js"></script>
    <script src="https://unpkg.com/element-ui/lib/index.js"></script>
    <script src="~/lib/axios/axios.js"></script>
    <script src="~/lib/echarts/echarts.min.js"></script>
    <!-- 自带当前页的样式 -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/HRUI.styles.css" asp-append-version="true" />
    @* 自定调用 *@
    <link href="~/css/starcss.css" rel="stylesheet" />
    @* <style>
        #gg{
            width:50px;
            height:50px;
            background-color:black;
            position:fixed;
            top:10px;
            left:10px;
        }
    </style> *@
</head>
<body>
    <div id="app">
        <div class="header">
            <!-- 页眉 -->
            @{
                Html.RenderPartial("Top.cshtml");
            }
        </div>

        <div id="left-link">
            <div class="sidebar bg-dark ">
                <!-- 侧边栏 -->
                @{
                Html.RenderPartial("Left.cshtml");
                }
            </div>
        </div>

        <div id="main-content-div">
            <div id="main-content" class="content">
                <!-- 主内容 -->
                @RenderBody()
            </div>
        </div>
    </div>
    <!-- 广告招租位 -->
 @*    <div id="gg">
        原神
    </div> *@
    <div id="star-picture">
        <!-- 开始展示的开场动画 -->
        欢迎登录人力资源管理系统
    </div>
    <script>
        function removeMB() {
            var myDiv = document.getElementById('star-picture');
            if (myDiv) {
                myDiv.parentNode.removeChild(myDiv);
            }
        }
        setTimeout(removeMB, "2000")
        // /**
        //  * 设置广告移动逻辑
        //  */
        // function ggmove(){
        //     var ggDiv = document.getElementById("gg");
        //     var top = 0;
        //     var left = 0;
        //     var topJ = true;
        //     var leftJ = true;
        //     while(true){
        //         ggDiv.style.position = 'fixed';
        //         if (topJ) {
        //             ggDiv.style.top = top + 'px';  /* 顶部距离 */
        //             top += 1;
        //             if (top >= 1480) {
        //                 topJ = false
        //             }
        //         } else {
        //             ggDiv.style.top = top + 'px';  /* 顶部距离 */
        //             top -= 1;
        //             if (top <= 0) {
        //                 topJ = true
        //             }
        //         }

        //         if (leftJ) {
        //             ggDiv.style.left = left+'px'; /* 左侧距离 */
        //             left += 1;
        //             if (left>=750) {
        //                 leftJ = false;
        //             }
        //         }else{
        //             ggDiv.style.left = left + 'px'; /* 左侧距离 */
        //             left += 1;
        //             if (left <= 0) {
        //                 leftJ = true;
        //             }
        //         }
        //     }
        // }
        // setTimeout(ggmove(),100)
    </script>
</body>
</html>
